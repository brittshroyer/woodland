{{#each model as |item|}}
  {{#link-to "orders.order" item}}
    <!-- passing item to order template -->
    Order {{item.id}} for {{item.name}}<br>
  {{/link-to}}
{{/each}}
<!-- iterate once for EACH item in the collection (model) -->
{{outlet}}
<!-- the outlet allows for child templates to be displayed -->
